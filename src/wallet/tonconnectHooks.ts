/**
 * Единственная точка входа библиотеки в `@tonconnect/ui-react`.
 *
 * Пять существующих игр не передают мост (`HudWallet`) в `HudProvider`, и для
 * них `useHudWallet` вызывает эти хуки как обычно. Но `useHudWallet` обязан
 * звать их безусловно (правила хуков не допускают условного вызова), а в
 * самодостаточном бандле scratch-game пакета `@tonconnect/ui-react` не будет
 * вовсе — там мост передан всегда. Решение: весь доступ к TonConnect собран
 * в этом одном тонком файле, и именно его сборка самодостаточного бандла
 * подменяет заглушкой (без `@tonconnect/ui-react` в зависимостях), которая
 * возвращает пустые значения. Мост в этом случае берёт всё на себя, и код
 * заглушки никогда не выполняется.
 */
export { TonConnectButton, useTonAddress, useTonConnectUI } from '@tonconnect/ui-react';
